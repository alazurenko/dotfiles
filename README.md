# dotfiles

Personal macOS configuration and development environment setup.

## Usage

```bash
./run          # Run all setup scripts (installs brew first, then all others)
./run --dry    # Dry-run mode to preview what would be executed
./run <filter> # Run only scripts matching the filter (e.g., ./run git)
```

Scripts are located in the `/runs/` directory.
